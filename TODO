* update to Go 1.1, once that's available on App Engine.

* handle "ping" in two ways: if it's from a golang/google person: make it
  be a mute (or a temporary logical close) until there's activity again

* add a codereview wait action? Q=wait

* in codereview, notice a R=foo after the last datastore task assign time 
  and reassign the qopher task to match

* in person.go, add aliases to people, e.g. people's code.google.com
  URLs like: gri@golang.org == /u/102602228801687104398/
